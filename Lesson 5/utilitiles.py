def calculate_everage(numbers):
    a = 0
    b = 0
    for num in numbers:
        a = a + num
        b = b + 1
    result = a / b
    return result

def find_max(numbers):
    result = max(numbers)
    return result

